#include <NKP_ONE.h>   //เรียกใช้ชุดคำสั่งทั้งหมด
void setup() {
  NKP_ONE();     //ประกาศใช้ชุดคำสั่ง
  IO15();       //รอจนกว่าจะกด SW1 เพื่อทำงาน

}

void loop() {
  set_oled_clear();    //เคลียหน้าจอ
  set_oled(0,0,"Knob = ");  //แสดงผลคำว่า "Knob = " ขึ้นที่หน้าจอ คอลัมที่ 0  บรรทัด ที่ 0
  set_oled(40,0,Knob());  //แสดงผลค่าที่ได้รับจาก ตัวต้านทานปรับค่าได้ ขึ้นที่หน้าจอ คอลัมที่ 30  บรรทัด ที่ 0
  delay(1);           //หน่วงเวลา  0.001 วินาที
  
}
