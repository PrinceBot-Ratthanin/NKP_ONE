#include <NKP_ONE.h>  //เรียกใช้ชุดคำสั่งทั้งหมด
void setup() {
  NKP_ONE();  //ประกาศใช้ชุดคำสั่ง
  IO15();     //รอจนกว่าจะกดปุ่ม SW1

}

void loop() {
  beep();    // กำเนิดเสียง ความถี่ 1 Hz    ส่งเสียง 500 ms และ ดับ 500 ms

}
