#include <NKP_ONE.h>   //เรียกใช้ชุดคำสั่งทั้งหมด
void setup() {
  NKP_ONE();     //ประกาศใช้ชุดคำสั่ง
  IO15();       //รอจนกว่าจะกด SW1 เพื่อทำงาน

}

void loop() {
  set_oled_clear();    //เคลียหน้าจอ
  set_oled(0,0,"SW1 = ");  //แสดงผลคำว่า "SW1 = " ขึ้นที่หน้าจอ คอลัมที่ 0  บรรทัด ที่ 0
  set_oled(30,0,sw1());  //แสดงผลค่าที่ได้รับจาก สวิทซ์ ขึ้นที่หน้าจอ คอลัมที่ 30  บรรทัด ที่ 0
  delay(1);           //หน่วงเวลา  0.001 วินาที
  
}
